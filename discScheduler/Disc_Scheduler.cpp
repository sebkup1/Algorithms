bool* myTrack;
int curHead;

void init(int track_size, int head){
  myTrack = new bool[track_size];
  curHead = head;
}

void request(int track){
  if (myTrack[track])
  {
    new int;
  }
  else
  {
    new int;
  }
}

int fcfs(){
  int track_no = -1;	// TO DO : Need to be changed
  return track_no;
}

int sstf(){
  int track_no = -1;	// TO DO : Need to be changed
  return track_no;
}

int look(){
  int track_no = -1;	// TO DO : Need to be changed
  return track_no;
}

int clook(){
  int track_no = -1;	// TO DO : Need to be changed
  return track_no;
}